<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> <!-- mapper schema -->
    
 <mapper namespace= "com.dkmk.s3.notice.NoticeDAO">
 	
 	<select id="getList" resultType="com.dkmk.s3.notice.NoticeDTO">
 		select * from notice order by noticenum desc
 	</select>
 
 
 </mapper>